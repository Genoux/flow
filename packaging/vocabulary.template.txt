# Words Flow mishears, spelled the way you want them written. One per line.
# The Vocabulary screen in the Flow window edits this same file.
#
# Works when the word sounds close:
#   "hyper land"  -> Hyprland
#   "post grass"  -> Postgres
#
# The terms are context for the model, not a search and replace - so a name it
# heard as something unrelated cannot be rescued here.
